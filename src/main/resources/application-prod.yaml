spring:
  datasource:
    url: ${DB_PROD_URL}
    username: ${DB_PROD_USER}
    password: ${DB_PROD_PASS}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

worklog:
  cors:
    allowed-origins: ${WORKLOG_CORS_ORIGINS}